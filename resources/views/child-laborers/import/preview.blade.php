<x-workspace-shell title="Import Preview" subtitle="Review validation results before inserting records.">
    <div class="space-y-6">
        <section class="grid gap-4 sm:grid-cols-4">@foreach([['Total',$batch->total_rows,'slate'],['Valid',$batch->valid_rows,'emerald'],['Duplicates',$batch->duplicate_rows,'amber'],['Failed',$batch->failed_rows,'red']] as [$label,$value,$color])<div class="rounded-2xl border border-{{ $color }}-200 bg-{{ $color }}-50 p-5"><p class="text-xs font-bold uppercase text-{{ $color }}-600">{{ $label }}</p><p class="mt-2 text-3xl font-black text-{{ $color }}-800">{{ $value }}</p></div>@endforeach</section>
        <section class="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><div class="flex flex-col gap-3 border-b border-slate-200 px-6 py-5 sm:flex-row sm:items-center sm:justify-between"><div><h2 class="text-lg font-black">{{ $batch->original_filename }}</h2><p class="text-sm text-slate-500">Only rows marked Valid will be imported.</p></div><div class="flex gap-2"><a href="{{ route('child-laborers.import.index') }}" class="rounded-xl border border-slate-300 px-4 py-2.5 text-sm font-bold">Cancel</a>@if($batch->valid_rows>0)<form method="POST" action="{{ route('child-laborers.import.confirm',$batch) }}">@csrf<button class="rounded-xl bg-emerald-600 px-4 py-2.5 text-sm font-black text-white">Confirm {{ $batch->valid_rows }} Valid Rows</button></form>@endif</div></div>
        <div class="max-h-[650px] overflow-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="sticky top-0 bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="px-4 py-3">Row</th><th class="px-4 py-3">Name</th><th class="px-4 py-3">Birth Date</th><th class="px-4 py-3">Location</th><th class="px-4 py-3">Result</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($rows as $row)<tr><td class="px-4 py-3">{{ $row['row'] }}</td><td class="px-4 py-3 font-bold">{{ $row['data']['first_name'] ?? '' }} {{ $row['data']['last_name'] ?? '' }}</td><td class="px-4 py-3">{{ $row['data']['birth_date'] ?? '' }}</td><td class="px-4 py-3">{{ $row['data']['barangay'] ?? '' }}, {{ $row['data']['city_municipality'] ?? '' }}</td><td class="px-4 py-3">@if(empty($row['errors']))<span class="rounded-full bg-emerald-100 px-3 py-1 text-xs font-bold text-emerald-700">Valid</span>@else<div class="space-y-1 text-xs font-semibold text-red-600">@foreach($row['errors'] as $error)<p>{{ $error }}</p>@endforeach</div>@endif</td></tr>@endforeach</tbody></table></div></section>
    </div>
</x-workspace-shell>
